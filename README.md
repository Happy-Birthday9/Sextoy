# Media Download

Updated PWA package. App display name: **Media Download**.

Deploy the contents with the `public` folder as the site root (Netlify: publish directory `public`, or use the included `netlify.toml`).

The Install App button uses Chrome's official `beforeinstallprompt` flow. A website cannot force-install an APK or bypass Android security; Chrome must decide when the site is installable.
